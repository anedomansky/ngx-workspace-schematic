// @ts-check

import tseslint from "typescript-eslint";
import { anStandardAngular, anStandardHTML, anStandardJSON } from '@anedomansky/eslint-config';

export default tseslint.config(
    ...anStandardAngular,
    ...anStandardHTML,
    ...anStandardJSON,
    {
        languageOptions: {
            parserOptions: {
                projectService: true,
                tsconfigRootDir: import.meta.dirname,
            },
        },
    },
);
