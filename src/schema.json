{
  "$schema": "http://json-schema.org/schema",
  "$id": "NewCompleteWorkspaceSchematic",
  "title": "ng new options schema",
  "type": "object",
  "description": "Initialize a new complete Angular workspace",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the workspace",
      "x-prompt": "Workspace name:"
    },
    "libraryPrefix": {
      "type": "string",
      "description": "The namespace of the library",
      "x-prompt": "Library namespace (e. g. @yourname):"
    },
    "libraryName": {
      "type": "string",
      "description": "The name of the library",
      "x-prompt": "Library name:"
    }
  },
  "required": ["name", "libraryName"]
}
