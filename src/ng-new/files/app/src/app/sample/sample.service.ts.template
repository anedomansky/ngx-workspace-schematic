import { Injectable } from '@angular/core';
<% if(type === 'complete') { %>import { SampleServiceConfig } from '@<%= dasherize(libraryPackageName) %>';<% } %>
import { Observable, of } from 'rxjs';

@Injectable()
export class <%= classify(appName) %>SampleService<% if(type === 'complete') { %> implements SampleServiceConfig<% } %> {
  sayHello(): Observable<string> {
    return of('Welcome from <%= dasherize(appName) %>!');
  }
}
