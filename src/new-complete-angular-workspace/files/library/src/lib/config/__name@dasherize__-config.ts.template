import { InjectionToken } from '@angular/core';

export type <%= classify(name) %>Config = {
  appTitle: string;
};

export function getDefault<%= classify(name) %>Config(): <%= classify(name) %>Config {
  return {
    appTitle: '<%= capitalize(name) %>',
  };
}

export const <%= classify(name) %>ConfigToken = new InjectionToken('<%= classify(name) %>Config');
